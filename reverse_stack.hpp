#include "util.hpp"

template<typename T>
void insert_to_bottom(T val, std::stack<T> &stack);

template<typename T>
void reverse_stack(std::stack<T> &stack) {
    /*
     * TODO: reverse the content of the stack without in O(1) space complexity
     */
}
