#include "util.hpp"

#define MOVE_DISK(disk, from, to) std::cout << "Move disk " << disk << " from plate " << from << " to plate " << to << std::endl

void move_items(int n, char station_from, char station_to) {
    /*
     * TODO: move items from station_from to station_to
     */
}

