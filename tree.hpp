#include "util.hpp"

template<typename T>
void print_tree(const TreeNode<T> *root) {
    /*
     * TODO: print all the tree nodes in different orders
     */
}

template<typename T>
TreeNode<T> *create_balanced_binary_search_tree(std::vector<T> &data) {
    /*
     * TODO: create a balanced binary tree using the given data
     */
}